server:
  port: 8080
spring:
  application:
    name: licensing-boot
eureka:
  instance:
    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://licensing-boot:8070/eureka/